package de.peekandpoke.kraft.addons.utils

// TODO: move to ultra-common-mp
fun <T> identity(it: T): T = it

// TODO: move to ultra-common-mp
fun numberToString(it: Number): String = it.toString()

// TODO: move to ultra-common-mp
fun stringToInt(it: String): Int = it.toInt()

// TODO: move to ultra-common-mp
fun stringToFloat(it: String): Float = it.toFloat()

// TODO: move to ultra-common-mp
fun stringToDouble(it: String): Double = it.toDouble()
